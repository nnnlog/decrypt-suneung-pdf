# 수능특강 PDF 편집 제한 해제, 워터마크 제거
* 프로그램 사용으로 발생하는 책임은 모두 본인에게 있습니다.

## 설치 (Ubuntu)
```
apt install pdftk nodejs
```

## 실행
1. `src/` 폴더에 pdf 파일들을 넣습니다.
2. `node index.js` 실행
3. `dist/` 폴더에 pdf 파일들이 저장됩니다.
